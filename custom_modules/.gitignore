# this is a build directory — ignore everything except the actual module
# source, which a fresh clone needs (the rest is regenerated by
# install_module.sh: .o/.ko/.mod.c/.symvers/modules.order/build-generated
# Makefile).
*
!.gitignore
!vsched_module.c
!Kbuild
